# public/.htaccess

# Active le moteur de réécriture
RewriteEngine On

# Empêche la réécriture si le fichier ou dossier demandé existe réellement sur le disque
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirige toutes les autres requêtes vers index.php
# Le [L] signifie "Last rule" (ne pas traiter d'autres règles si celle-ci correspond).
RewriteRule ^ index.php [L]